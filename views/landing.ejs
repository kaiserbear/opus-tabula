<% include partials/header %>

<div class="container">
    <div class="row">

		<h1>Landing Page!</h1>

		<p>Welcome to Job Camp</p>

		<a href="/jobs">View All Jobs</a>

		<% if (currentUser) { %>
			<h2>Hello, <%= currentUser.user.linkedIn.name %>.</h2>
			<p><%= currentUser.profile._json.summary %></p>
			<pre><code><%= JSON.stringify(currentUser, null, 2) %></code></pre>
		<% } else { %>
			<p>Not logged in</p>
		<% } %>

	</div>
</div>

<% include partials/footer %>